- if @current_user.has_active_participants?
  .row-fluid
    .well.well-small
      %h4="Participants"
    %table.table.table-condensed.table-striped
      %thead
        %tr
          %th="Name"
          %th="Join Date"
          %th="Status"
          %th="Options"
      %tbody
        - @current_user.active_participants.each do |participant|
          %tr
            %td=link_to participant.name, edit_participant_path(participant)
            %td=participant.consent_signatures.last.created_at.to_date
            %td
              %ul.unstyled
                -if participant.open?
                  %li
                    Incomplete
                  %li
                    =link_to "Click here to finish", enroll_participant_path(participant)
                -else
                  %li
                    Complete
            %td
              %ul.unstyled
                - if participant.consented?
                  %li
                    %i.icon-folder-open
                    =link_to "View Consent", enroll_participant_path(participant)
                - if participant.completed?
                  %li
                    %i.icon-folder-open
                    =link_to  "View questionarie", enroll_participant_path(participant)
                %li
                  %i.icon-trash
                  = link_to "Withdraw", withdraw_participant_path(participant), method: :patch, confirm: "Are you sure you want to withdraw?"