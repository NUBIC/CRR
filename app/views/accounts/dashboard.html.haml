Welcome to the CRR. Please add yourself or someone else to the registry.

%ul.nav.nav-tabs
  %li{:class=>'active'}=link_to 'Dashboard','#dashboard',"data-toggle"=>'tab'
  %li=link_to 'Edit Account','#account',"data-toggle"=>'tab'
  %li=link_to 'Existing Participants','#participants',"data-toggle"=>'tab'
.tab-content
  #dashboard.tab-pane.active
    .row-fluid
      =form_tag(participants_path, :method => 'post') do
        = hidden_field_tag "account_id", @account.id
        = submit_tag('Add yourself', :class=>"btn btn-mini btn-primary", :confirm => "Are you sure you want to add yourself?")
      =form_tag(participants_path, :method => 'post') do
        = hidden_field_tag "account_id", @account.id
        = hidden_field_tag "proxy", true
        = submit_tag('Add Child', :class=>"btn btn-mini btn-primary", :confirm => "Are you sure you want to add Child?")
  #account.tab-pane
    = render :partial => 'account_update'
  #participants.tab-pane
    = render :partial => 'account_participants'