- if ((Consent.child_consent || Consent.adult_consent) && (Survey.adult_survey || Survey.child_survey))
  .well.well-small
    %h4
      Add a participant
  %h5
    Who are you enrolling?
  .row-fluid
    - if recruiting_adults?
      .span2
        = form_for @participant, url: admin_participants_path do |f|
          = f.hidden_field 'child', value: false
          = f.button 'Add Adult', class: 'btn btn-medium btn-primary nu-primary'
    - if recruiting_children?
      .span2
        = form_for @participant, url: admin_participants_path do |f|
          = f.hidden_field 'child', value: true
          = f.button 'Add Child', class: 'btn btn-medim btn-primary nu-primary'
- else
  %p
    Please make sure you have active consents and surveys in the system. You can't enroll new participant without active consent and survey.